import { ImgEquipmentPipe } from './img-equipment.pipe';

describe('ImgEquipmentPipe', () => {
  it('create an instance', () => {
    const pipe = new ImgEquipmentPipe();
    expect(pipe).toBeTruthy();
  });
});
