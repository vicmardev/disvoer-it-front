import { PageTitlePipe } from './page-title.pipe';

describe('PageTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new PageTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
